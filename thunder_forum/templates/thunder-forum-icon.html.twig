{#
/**
 * @file
 * Default theme implementation to present a forum term/node icon.
 *
 * Available variables:
 *   - entity: The entity to display an icon for.
 *   - identifier. The icon identifier.
 *   - locked: Whether the icon has to reflect the 'locked' status.
 *   - hot: Whether the icon has to reflect the 'hot' status.
 *   - new: Whether the icon has to reflect the 'new' status.
 *   - own: Whether the icon has to reflect that the current user is the owner.
 *
 * @see template_preprocess_thunder_forum_icon()
 *
 * @ingroup themeable
 */
#}
{{- identifier -}}
