{#
/**
 * @file
 * Default theme implementation to present forum user's post count.
 *
 * Available variables:
 *   - reply_count: The number of forum replies the user created so far.
 *   - topic_count: The number of forum topics the user created so far.
 *   - sum_count: The summed up count for all forum posts the user created so
 *     far.
 *   - uid: The ID of the user to display the count for.
 *
 * @ingroup themeable
 */
#}
{{- sum_count -}}
