langcode: en
dependencies:
  enforced:
    module:
      - thunder_notify
id: logger
message: >
  "{user:name}" received the following notification:

  {messages}

  ==================================================

  {site:name}: {site:url}
subject: 'New notification for "{user:name}" ({user:uid})'
